import AuthenticationLayout from './AuthenticationLayout'

export const generated = (args) => {
  return <AuthenticationLayout {...args} />
}

export default { title: 'Layouts/AuthenticationLayout' }
